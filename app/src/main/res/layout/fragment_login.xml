<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_background">

    <TextView
        android:id="@+id/login_enter_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/enter"
        android:textSize="36sp"
        android:textColor="@color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="70dp"/>

    <TextView
        android:id="@+id/login_email_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/e_mail"
        android:textColor="@color/white"
        android:textSize="16sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="@id/login_text_input_id"
        android:layout_marginTop="150dp"
        android:layout_marginStart="5dp"
        />

    <EditText
        android:id="@+id/login_text_input_id"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:background="@drawable/animal_search_panel"
        app:layout_constraintTop_toBottomOf="@id/login_email_id"
        android:layout_marginStart="60dp"
        android:layout_marginEnd="60dp"
        android:padding="10dp"
        android:autofillHints="Введите логин" />

    <TextView
        android:id="@+id/login_password_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/password"
        android:textColor="@color/white"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/login_text_input_id"
        app:layout_constraintStart_toStartOf="@id/login_email_id"
        android:layout_marginTop="25dp"/>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/password_input_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/animal_search_panel"
        app:layout_constraintTop_toBottomOf="@id/login_password_id"
        android:layout_marginStart="60dp"
        android:layout_marginEnd="60dp"
        >

        <EditText
            android:id="@+id/login_password_input_id"
            android:layout_width="0dp"
            app:layout_constraintWidth_percent="0.85"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            app:layout_constraintEnd_toStartOf="@id/password_visibility"
            android:layout_height="45dp"
            android:inputType="textPassword"
            android:autofillHints="Введите пароль"
            android:background="@null"
            />

        <ToggleButton
            android:id="@+id/password_visibility"
            android:layout_width="30dp"
            android:layout_height="30dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/login_password_input_id"
            android:layout_marginEnd="5dp"
            android:background="@drawable/password_visibility_button"
            android:textOff=""
            android:textOn="" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <Button
        android:id="@+id/login_fragment_start_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/enter"
        android:layout_marginTop="25dp"
        android:layout_marginStart="80dp"
        android:layout_marginEnd="80dp"
        android:layout_marginBottom="10dp"
        android:backgroundTint="#FF8A80"
        app:layout_constraintTop_toBottomOf="@id/password_input_container"/>




</androidx.constraintlayout.widget.ConstraintLayout>